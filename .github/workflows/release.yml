name: "CD: Auto-Tag & GitHub Release"

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  release:
    name: "🏷️ Create New Version & Release"
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
          
      - name: "🔢 Calculating Next Semantic Version"
        id: version
        run: |
          LATEST_TAG=$(git describe --tags --abbrev=0 2>/dev/null || echo "v0.0.0")
          echo "Latest tag: $LATEST_TAG"
          # Extract major, minor, patch
          IFS='.' read -r major minor patch <<< "${LATEST_TAG#v}"
          # Default bump is patch
          patch=$((patch + 1))
          NEXT_TAG="v${major}.${minor}.${patch}"
          echo "Next tag: $NEXT_TAG"
          echo "previous_tag=$LATEST_TAG" >> $GITHUB_OUTPUT
          echo "tag=$NEXT_TAG" >> $GITHUB_OUTPUT

      - name: "🖊️ Tagging Repository"
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git tag ${{ steps.version.outputs.tag }}
          git push origin ${{ steps.version.outputs.tag }}

      - name: "📝 Generating Release Notes"
        run: |
          python3 scripts/generate_release_notes.py \
            --repo-key postgres-wire \
            --repo-name postgres-wire \
            --new-tag "${{ steps.version.outputs.tag }}" \
            --previous-tag "${{ steps.version.outputs.previous_tag }}" \
            --output release-notes.md

      - name: "🚀 Creating GitHub Release"
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ steps.version.outputs.tag }}
          name: Release ${{ steps.version.outputs.tag }}
          body_path: release-notes.md
